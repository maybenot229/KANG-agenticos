import EmptyState from "../common/EmptyState";

/**
 * Learn domain (09_UI §2/UI-001). `src/kang/domain/learning/` is an empty
 * stub — spaced repetition and the Tutor agent are Phase 2 (M7)
 * objectives (`03_ROADMAP.md`), not built yet.
 */
export default function LearnScreen() {
  return (
    <EmptyState
      label="Learn"
      heading="Learn"
      message="The learning domain has no backend yet — spaced repetition and tutoring are later-phase work (Phase 2 of the roadmap)."
    />
  );
}
