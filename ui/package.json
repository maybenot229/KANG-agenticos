{
  "name": "kang-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "KANG dashboard — pure API client (UI-P1). No truth, no domain logic.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "generate": "node scripts/generate-client.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "json-schema-to-typescript": "^15.0.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.21"
  }
}
