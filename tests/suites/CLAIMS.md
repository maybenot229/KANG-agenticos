# CLAIMS.md — Constitutional Claims Under Test

Registry mandated by 14_CLAUDE §7 / 03_ROADMAP §1.6: every suite starts from
a constitutional MUST and proves *that claim*. Updated in the same PR as any
claim-bearing test. One line per claim; the test proves it or the line lies.

| Claim (decision / MUST) | Proven by |
|---|---|
| 17 §4.2/§4.3 — imports point inward only; the named forbidden vectors are individually red | `suites/architecture/test_import_contracts.py` (green on real code + red-test: deliberate violation fails) |
| 17 §4.4 — a package without a contract entry is red by omission | `suites/architecture/test_import_contracts.py::test_every_src_package_has_a_contract_entry` |
| 17 §4.3.6 — no cross-adapter imports | import contract "adapter tech folders are independent" + independence-entry test |
| 17 §4.3.9 — production never imports tests/ or tools/ | `suites/architecture/test_import_contracts.py::test_no_production_import_of_tests_or_tools` |
| 11 §4 — size hard limits fail CI | `suites/architecture/test_architecture_lints.py` (green + planted-violation red-tests) |
| 11 §25 — print/SQL-outside-store/wall-clock/os.environ bans | `suites/architecture/test_architecture_lints.py` (per-pattern red-tests; os.environ config exemption proven) |
| PS-002 — zero runtime state, zero secrets in the repo | `suites/architecture/test_architecture_lints.py` (tree hygiene green + .db/secret red-tests) |
| 14 header — root CLAUDE.md is a generated copy, never hand-edited | `suites/architecture/test_architecture_lints.py::test_root_claude_md_is_generated_and_current` |
| 07 Part XIII.4 — a modified historical migration is startup-blocking | `integration/sqlite/test_migrations.py::test_modified_historical_migration_blocks_startup` |
| 07 Part XIII.1 — migrations are NNNN_description.sql, gapless, checksummed | `integration/sqlite/test_migrations.py` (discover + checksum tests) |
| DB-003 — no partial truth: a failed migration leaves nothing behind | `integration/sqlite/test_migrations.py::test_failed_migration_leaves_no_partial_truth` |
| D009 — sync quartet on every synchronizable row from migration 0001 | `integration/sqlite/test_task_store.py::test_task_table_carries_the_quartet_columns` + contract quartet test |
| 07 §5.6 — every write is change-captured (insert/update/delete), ordered by seq | `integration/sqlite/test_task_store.py` capture tests |
| 07 §5.1 — deletes tombstone | `integration/sqlite/test_task_store.py::test_delete_is_captured_and_tombstoned` |
| DB-001/DB-003 — optimistic revision checks; stale writes conflict, revisions bump | `fixtures/task_store_contract.py` (run against fake AND sqlite) |
| 13 §2.3 — fakes are contract-paired with real adapters | `unit/.../test_task_store_fake.py` + `integration/sqlite/test_task_store.py` share `TaskStoreContract` |
| 11 §14 — the clock is injected; domain stamps time only through it | `unit/kang/domain/tasks/test_task_service.py` (FakeClock determinism) |
| 12 §5 / D015 — one correlation id threads the execution context; logs are JSON lines | `unit/kang/kernel/runtime/test_structured_logging.py` |
| D003 / PS-002 — %KANG_HOME% resolves from environment; no silent default into the checkout | `unit/kang/adapters/config/test_env_config.py` |
| 12 §16 — the registry is machine-readable, deterministic, the contract's source of truth | `unit/kang/api/registry/test_registry.py` |
| SEC-013 — audit is append-only JSONL, monthly, hash-chained per file; tampering is evident | `fixtures/audit_log_contract.py` (fake + jsonl) + `integration/jsonl/test_audit_log.py` (bit-flip and deletion break the chain loudly — 13 §2.9) |
| SEC-006 — anonymous action is architecturally impossible; every entry carries principal + correlation id | `unit/kang/kernel/audit/test_service.py` |
| 15 §5.1/§5.2 — the envelope is a closed field list; the eventlog DDL is exact; nothing invalid enters the log | `fixtures/event_log_contract.py` (fake + sqlite) + `integration/eventlog/test_event_log.py` (DDL column-exact, indexes cited) |
| EB-003 / DB-001 — eventlog runs synchronous=FULL on its own connection; recovery-grade payloads are self-sufficient | `integration/eventlog/test_event_log.py::test_connection_runs_synchronous_full` + contract self-sufficiency rejection test |
| 15 §4 — pending → confirmed \| orphaned; orphans are never deleted | `fixtures/event_log_contract.py` state-machine tests |
| 07 Part XII — VACUUM INTO is the only backup method; integrity gate before snapshot; corruption detected loudly | `integration/sqlite/test_backup.py` |
| EB-003 — re-application is idempotent, keyed by entity id + revision | `suites/replay/test_crash_replay.py::test_recovery_is_idempotent_run_twice` + `suites/backup_restore/test_restore_drill.py::test_restore_replay_is_idempotent` |
| **C1 (18 §3 M1)** — kill between every M1 write-order step pair ⇒ reconciliation converges, zero partial truth (13 §2.5) | `suites/replay/test_crash_replay.py` (subprocess fault injection, all four kill points + clean run) |
| **C1 (18 §3 M1)** — snapshot → corrupt live → restore → field-equality → gap replay (13 §2.15) | `suites/backup_restore/test_restore_drill.py::test_the_c1_restore_drill` |
| EB-004 — the five-step write order is event-first; a lost state commit becomes a recoverable ghost, never a silent miss | `suites/replay/test_crash_replay.py` (real `EventBus.publish` killed at every boundary) |
| EB-004 §4 — the caged reconciliation pass: re-apply recovery-grade & confirm; confirm-or-orphan the rest; report the window | `unit/kang/kernel/bus/test_reconciliation.py` |
| EB-007 — per-subscriber cursors, FIFO by seq, retry ×5 → dead-letter, cursor advances past a poison event; consumers dedup on event_id | `unit/kang/kernel/bus/test_delivery.py` (incl. poison-event §16.4) |
| EB-007 — cursors are delivery truth, per-subscriber, monotonic, durable across reopen | `fixtures/delivery_store_contract.py` (fake + sqlite) + `integration/eventlog/test_delivery_store.py` |
| EB-011.1 — a declared event→job→event cycle is rejected and named (static lint) | `unit/kang/kernel/bus/test_cycle_defense.py` |
| EB-011.2 — a causation chain deeper than 16 must not publish further (runtime depth guard) | `unit/kang/kernel/bus/test_cycle_defense.py` |
| §6.3 — publishing an unregistered type is rejected; recovery_grade is the registry's not the publisher's; payload schema checked | `unit/kang/kernel/bus/test_event_registry.py` |
| §16.2 / EB-003 — every recovery-grade type's payload reconstructs its row on an empty store (registry-driven — a new type without the proof fails CI) | `suites/replay/test_payload_sufficiency.py` |
| **C2 (18 §3 M2)** — kill between every EB-004 step pair ⇒ the REAL reconciliation + delivery resume converge, zero partial truth, at-least-once + idempotent delivery (13 §2.5) | `suites/replay/test_crash_replay.py` |
| SEC-004 / D013 — authority is capability-based, default-deny; a denial names the missing scope and mutates nothing | `unit/kang/kernel/permissions/test_engine.py` (property suite 13 §2.7) + `test_scope.py` |
| 05 §8 / 10 §2.6 — over-privilege is a load-time failure: non-kang wildcard, web.fetch×sensitive/vault.write, ungrantable memory.propose:rule\|profile | `unit/kang/kernel/permissions/test_pairing.py`; run at engine build via `build_checked_engine` |
| EB-010 checkpoint 1 — **publish authority** (`events.publish:{namespace}`) gates EB-004 step 1; an unauthorized publish is denied and nothing is appended (**M2 deferral 1 closed**) | `unit/kang/kernel/bus/test_publish_authority.py` |
| 07 F8 — permissions.toml is grant truth; corrupt/missing fails closed to Kang-only | `unit/kang/adapters/config/test_permissions_loader.py` |
| 12 §7 — held actions are data: what/who/why/reversibility + 24h expiry + status; past-expiry approval refused | `fixtures/held_action_store_contract.py` (fake + sqlite) |
| `held_action.approve`/`.cancel` (added 2026-08-05, closing the confirmed-open gap from the 2026-07-31 audit) — the handlers drive the already-built `HeldActionStore` through `pending -> approved \| cancelled`, map its typed failures to `not_found`/`conflict`, and add nothing the store doesn't already enforce; `commit_mode="transactional"` registered (ADR-001 Amendment, the other confirmed-open gap). Driving an approved action to `executed` remains open — no live operation is on 05_AGENTS Appendix D's closed list yet, and the row has no stored params to replay — **closed 2026-08-13 by ADR-021, see below** | `unit/kang/api/test_held_action_operations.py` (handler-level) + `unit/kang/api/test_dispatch.py::test_plugin_session_is_refused_first_party_only_operation`/`test_first_party_session_may_call_first_party_only_operation` (channel-level, pre-existing, now exercising a real registered operation) |
| `permission.list` (added 2026-08-05, 09_UI §7's System-domain permission screen — "what can KANG touch?") — reflects `PermissionEngine.snapshot()` verbatim (every principal, every scope in `permissions.toml`'s own string form) with a plain-language consequence sentence per scope family, falling back honestly rather than fabricating one for a family this session didn't write a description for. Read-only: no `grant.modify` exists, so viewing cannot become editing by accident | `unit/kang/kernel/permissions/test_engine.py::TestSnapshot` (engine-level) + `unit/kang/api/test_permission_operations.py` (handler-level) |
| `held_action.list` + the confirm dialog (added 2026-08-05, 09_UI §4/§7's approval queue and confirm dialog) — `held_action.list` exposes `HeldActionStore.pending()` verbatim (oldest first, drops off on approve/cancel); the dialog renders exactly one held action's what/who/why/reversibility (12_API §7), Deny holds keyboard focus on mount (never Approve — no Enter-through), Escape denies in one keystroke without asking why. Live-verified against a real Core: seeded held actions approved/denied through the actual rendered dialog (not just the API underneath it) committed the correct final status, confirmed in `kang.db` and the audit trail | `unit/kang/api/test_held_action_operations.py::TestList` (handler-level; the dialog's own keyboard-focus/Escape/idempotency-key behavior has no automated UI-interaction test yet — verified live this session, same known gap as quick capture's DOM layer noted in the 2026-08-05 handoff) |
| `audit.list`/`system.health` (added 2026-08-05, 09_UI §12's Activity and Health System-domain views) — both expose pre-existing store methods verbatim (`AuditService.records`/`.months`, new thin pass-throughs over `AuditLog`; `JobStore.list_jobs`/`.consecutive_failures`; `KillSwitch.is_engaged`), no new domain logic. Required moving `job_store`/`kill_switch` construction in `composition.py` out of `_wire_scheduler` (which only ran when `kang.toml` loaded) so Health works whether or not automation is configured — live-verified both ways (no `kang.toml`: empty jobs, `automation_engaged: false`; real `kang.toml`: the real `morning_plan` job listed) against a real Core over real HTTP | `unit/kang/kernel/audit/test_service.py::TestReadPassThroughs` + `unit/kang/api/test_system_operations.py` |
| D014 / 05 §11 — three catch-up policies converge; ≥3 consecutive failures quarantine; disabled/quarantined jobs don't dispatch | `unit/kang/kernel/scheduler/test_scheduler.py` + `test_schedule.py` |
| D013 — the kill-switch pauses all automation and persists across restart | `unit/kang/kernel/scheduler/test_scheduler.py` + `integration/sqlite/test_job_store.py` |
| EB-007.4 — a failed handler's redelivery **waits the exponential backoff** (0.5,1,2,4) instead of firing immediately (**M2 deferral 2 closed**) | `unit/kang/kernel/bus/test_delivery.py::test_retries_wait_the_exponential_backoff_between_attempts` |
| **C3 (18 §3 M3)** — catch-up under fault injection: kill mid-pass ⇒ the REAL scheduler resumes; every policy converges (run_all_missed once-per-slot, no double-run/none-lost; run_once_latest once; skip none) (13 §2.5) | `suites/replay/test_scheduler_catchup.py` |
| 12 §16 — the operation/event/error registries are served, deterministic, the contract's source of truth | `unit/kang/api/registry/test_registry.py` |
| API-006 — every failure returns the one envelope; `code` is the closed enum; unknown codes rejected | `unit/kang/api/test_errors.py` + `unit/kang/api/test_dispatch.py` |
| 12 §5 — the request lifecycle: session→principal (API-003) → validate → idempotency (API-004) → permission (M3 engine) → execute → response, correlation_id at ingress | `unit/kang/api/test_dispatch.py` |
| API-003 — authentication only; no valid session ⇒ refused; authorization is entirely the engine's | `unit/kang/api/test_dispatch.py::test_no_session_is_refused` + `test_permission_denied_names_the_scope` |
| API-004 — a repeated idempotency key returns the original outcome, never re-executes | `unit/kang/api/test_dispatch.py` + `integration/sqlite/test_api_stores.py` (first-write-wins) |
| 12 §12 / 15 §8.3 — explain.invocation reconstructs from invocation + audit in PERMANENT storage, never the event log | `suites/explainability/test_c4_kang_explain.py` (reconstructs after a full Core restart) |
| 12 §12 / A4 — reconstruction impossible ⇒ honest not_found, never a synthesized narrative | `suites/explainability/test_c4_kang_explain.py::test_c4_explain_unknown_correlation_is_honest_not_found` |
| **C4 (18 §3 M4)** — first working `kang explain`: a task created THROUGH the CLI (separate process, no core imports) is reconstructed end-to-end by explain.invocation from persistent storage — proven across a Core restart (05 §14) | `suites/explainability/test_c4_kang_explain.py` |
| 07 §5.2 — the deadline entity: kinds, statuses, and the anchor CHECK (a non-school/custom deadline MUST reference a competition or project) | `unit/kang/domain/deadlines/test_deadline_service.py` + `integration/sqlite/test_deadline_store.py` constraint tests |
| FR-031 — lead-time alerting is configurable (`lead_days`) and deterministic: thresholds cross in a fixed descending order against an injected `now` | `unit/kang/domain/deadlines/test_deadline_service.py::TestLeadThresholds` |
| 07 §4.1 — `v_active_deadlines`' read shape: only `tracked`, ordered `(at, id)` — a deterministic total order so identical state yields identical Planner input (13 §2.6) | `fixtures/deadline_store_contract.py` (fake + sqlite) |
| 07 Part XIII.5 — 0006 recreates `task` to add its deferred FK **losslessly**; rows, capture triggers, and the new FK all survive | `integration/sqlite/test_migrations.py::test_0006_preserves_task_rows_across_the_table_recreation` + `test_0006_task_project_fk_is_enforced` + `test_0006_task_change_capture_still_fires_after_recreation` |
| EB-003 / 15 §16.2 — **every** recovery-grade type's payload reconstructs its row on an empty store, and re-application is idempotent; a recovery-grade type without a fixture fails CI (registry-driven, so future types cannot skip the guarantee) | `suites/replay/test_payload_sufficiency.py` (now covers `deadline.created`/`deadline.updated` alongside the task types) |
| **ADR-004 ordering ruling** — one `tracked → alerted` flip publishes `deadline.updated` (state commit, recovery-grade) **then** `deadline.approaching` (pure fact, `causation_id` = the mutation's event_id), never one alone and never reversed | `unit/kang/api/test_deadline_operations.py::TestSweepOrdering` |
| EB-006 §6.3 — a recovery-grade type MUST have a re-application path; registering `deadline.*` obliges its applier | `adapters/sqlite/recovery.py` appliers + `suites/replay/test_payload_sufficiency.py::test_every_recovery_grade_type_has_a_fixture` |
| **ADR-005** — the notification queue is the durable work item (15 §6.2), per-device operational state with **no sync quartet**, and acks are additive (`acked_at`), never destructive (12 §13) | `fixtures/notification_store_contract.py` (fake + sqlite) + `integration/sqlite/test_notification_store.py::test_notification_carries_no_sync_quartet` |
| 09_UI §9 / 05 §13 — the interruption ladder maps priority → delivery, and an unknown priority raises rather than silently not notifying | `unit/kang/domain/notifications/test_notifier.py::TestLadder` |
| 09_UI §9 — re-notification of an unchanged item within 24h is core-enforced; only notifications that were **actually surfaced** count as the prior one (a `queued` or `suppressed` row must never suppress a later one, or two enqueues silence each other) | `unit/kang/domain/notifications/test_notifier.py::TestDuplicateRule` + `fixtures/notification_store_contract.py` recent-matching tests |
| 15 §5.1 / EB-007.2 — a subscriber MAY publish (that is what `causation_id` is for) without the fan-out recursing over the in-flight, not-yet-acked event | `unit/kang/kernel/bus/test_fan_out_reentrancy.py` |
| **M5 Proves (18 §3)** — deadline lifecycle end to end, fully offline, zero model calls: create → approach event → notification per ladder → acknowledge, against the REAL wired Core | `suites/determinism/test_deadline_to_notification.py` |
| **FR-001 / 05 §16 — RELEASE-BLOCKING** — the deterministic Planner: same inputs ⇒ byte-identical plan and byte-identical ordering; input order cannot change the plan; the id tiebreak makes the order *total*; a plan exists with zero tasks, zero deadlines and no calendar provider (NFR-002) | `suites/determinism/test_plan_determinism.py` |
| 18 §7.6 — the deterministic path contains no model: structurally unreachable, asserted rather than assumed (no provider adapter wired, no `model_call` ledger) | `suites/determinism/test_plan_determinism.py::TestDeterminism::test_no_model_or_clock_is_reachable_from_the_planner` + `suites/replay/test_simulated_week.py::test_the_week_makes_zero_model_calls` |
| 05 Appendix E — trigger times are **config, not spec**: read from the shipped `kang.toml` (05:45 six school days, 06:45 Sunday), never hardcoded; Saturday is a school day, so the week has seven planned mornings | `suites/replay/test_simulated_week.py::TestTriggerTimes` |
| **M5 GATE (18 §3)** — a full simulated week produces every morning plan with zero model calls, reproducibly, and plan-date stamping is idempotent across a replayed slot (`run_once_latest`) | `suites/replay/test_simulated_week.py::TestSimulatedWeek` |
| **ADR-006 / D014** — wall-clock (`cron:`) schedules express Kang's real routine (05:45 Mon–Sat, 06:45 Sun, `Asia/Kuching`); the timezone genuinely applies (a different zone moves the instant); malformed expressions are refused at load rather than firing at an unchosen time | `unit/kang/adapters/scheduler/test_cron.py` |
| **ADR-006** — `morning_plan` is registered from CONFIG not code, idempotently across reboots, with `run_once_latest`; five missed mornings collapse to one plan; a freshly-registered job does not fire retroactively | `suites/replay/test_morning_plan_job.py::TestRegistration` + `::TestCatchUp` |
| **ADR-006 / SEC-003 / ADR-002** — a job dispatches through the ordinary pipeline as `kernel:scheduler` with **`first_party=False`**, so automation is structurally unable to approve a held action; the dispatch is invocation-recorded, so scheduled work is explainable (12 §12) | `suites/replay/test_morning_plan_job.py::TestDispatch` |
| 07 F8's shape, generalized — missing `kang.toml` disables automation and audits why, without bricking the Core; never an invented schedule, never a dead system (ADR-006 Amendment) | `suites/replay/test_morning_plan_job.py::TestDegradation` |
| **ADR-010 Ruling 4** — a request failing schema validation returns `invalid_request` with sanitized `field_errors` (field path + message type only); no raw offending value ever reaches the response, for every operation currently carrying a `request_schema`, generically | `unit/kang/api/test_dispatch.py` (single-case behavior) + `suites/contract/test_schema_validation_conformance.py` (swept across the whole registry) |
| API-002 / 12_API §1.3 — the local HTTP binding answers a CORS preflight (OPTIONS) with 204 + `Access-Control-Allow-*`, and every response (success, error, unknown-path) carries the same headers, so a browser-engine client (Tauri's webview) can actually read the response instead of failing the fetch outright | `unit/kang/api/test_http_binding.py` (found by a real browser client against a real running Core, session 2026-08-04 — the binding had zero test coverage before this) |
| `deadline.list` (added 2026-08-05, dashboard Zone 2, 09_UI §4) — exposes `DeadlineStore.active()` verbatim (tracked-only, soonest-first) through the API for the first time; no filtering/ordering logic of its own, and an alerted deadline drops off the list the same moment `deadline_sweep` stops re-alerting it | `unit/kang/api/test_deadline_operations.py::TestList` |
| `invocation.list` (added 2026-08-05, System-domain Invocations view, 09_UI §12) — genuinely new port surface, not pure exposure (`InvocationStore` had only `by_correlation`, a point lookup, before this): `recent(limit)` orders `started DESC, id DESC` (indexed by migration 0010), the handler defaults `limit` to 50 and clamps to 500 (12_API §15's standing page limits) never lets a non-positive `limit` fall through to SQLite's `LIMIT -1`/`0` special-casing, and list rows carry no `manifest` field (that stays behind `explain.invocation`, one click away per 09_UI §12's "each row opens `kang explain`"). Not cursor-paginated — API-008 names cursor pagination as the default for "all list queries"; this is a bounded single page, a named open gap alongside `deadline.list`/`held_action.list`/`audit.list`'s own non-cursor precedent, not silently completed. Live-verified against a real Core: seeded multiple invocations via real API calls, confirmed newest-first ordering and limit-clamping over real HTTP | `unit/kang/api/test_invocation_operations.py` (handler-level) + `integration/sqlite/test_api_stores.py::test_invocation_recent_orders_newest_first_and_respects_limit` (real SQLite ordering/limit) |
| `project.create`/`.list` (added 2026-08-06, ADR-013, Projects domain — tracking only per 03_ROADMAP M4/M5) — the Projects domain's first real write path: `project.create` validates name/status as typed domain errors before persistence, publishes the newly-registered `project.created` (recovery-grade, full row, ADR-013) under `kernel:projects`, and commits the row only inside that publish (EB-004: `commit_state` runs nowhere else); `project.list` exposes `ProjectStore.list_all()` verbatim, name-then-id ordered. Migration 0011 gave `project` its first change-capture trigger (0006 never wired one, since nothing wrote to it before this) — proven firing, not assumed. `project.updated` deliberately not registered (no status-transition operation exists yet — the same non-speculation discipline ADR-004 used for `competition.*`) | `unit/kang/domain/projects/test_project_service.py` (validation/payload) + `unit/kang/api/test_project_operations.py` (handler-level) + `integration/sqlite/test_project_store.py` (real SQLite ordering + change_log row) + `suites/replay/test_payload_sufficiency.py` (project.created's payload-sufficiency fixture, EB-003/13 §16.2) |
| `competition.create`/`.list` (added 2026-08-06, ADR-014, Competitions domain — tracking only per 03_ROADMAP M4/M5) — closes ADR-004's other explicitly-deferred item (the `project` half closed by ADR-013 the same session): the Competitions domain's first real write path, identical shape to `project.create`/`.list`. `evaluation`/`result` are never written by this path — always `null` in the payload — since those are Phase 3's own write path (07 §5.2's own comment on the table), proven by test, not just asserted in the ADR. Migration 0012 gave `competition` its first change-capture trigger (0006 never wired one, same gap `project` had). `competition.updated` deliberately not registered, same non-speculation discipline | `unit/kang/domain/competitions/test_competition_service.py` (validation/payload/evaluation-result-always-null) + `unit/kang/api/test_competition_operations.py` (handler-level) + `integration/sqlite/test_competition_store.py` (real SQLite ordering + change_log row) + `suites/replay/test_payload_sufficiency.py` (competition.created's payload-sufficiency fixture, EB-003/13 §16.2) |
| `milestone.create`/`.list` (added 2026-08-07, ADR-015, Milestones sub-domain under Projects — tracking only) — third instance of the `project`/`competition.created` pattern this session, same shape: recovery-grade full-row payload, migration 0013 gave `milestone` its first change-capture trigger, `milestone.updated` deliberately not registered. Two things unique to milestone, both proven by test rather than assumed: `project_id` is `NOT NULL REFERENCES project(id)` (unlike deadline's nullable anchors), so the payload-sufficiency fixture seeds a real parent project row first — "empty store" here means "empty except the FK this row structurally cannot exist without," matching what a real crash-recovery replay would actually see; and `project -> milestone` is a pre-sanctioned CASCADE (07_DATABASE Appendix B) — proven that deleting a project both removes its milestones AND fires the milestone's own change-capture trigger for the FK-driven delete (SQLite fires AFTER DELETE triggers for FK actions by default since 3.6.18; this project had never exercised that path before, so it was verified rather than assumed) | `unit/kang/domain/projects/test_milestone_service.py` (validation/payload) + `unit/kang/api/test_milestone_operations.py` (handler-level, including project_id-required) + `integration/sqlite/test_milestone_store.py` (real SQLite ordering, unknown-project FK rejection, CASCADE-delete-fires-trigger) + `suites/replay/test_payload_sufficiency.py` (milestone.created's payload-sufficiency fixture with seeded parent, EB-003/13 §16.2) |
| `goal.create`/`.list` (added 2026-08-08, ADR-016, fourth instance of the `project`/`competition`/`milestone.created` pattern — this time the standing ADR ADR-015 itself flagged, generalizing the rule rather than re-arguing it a fourth time) — `goal` is self-standing (no required FK, like `project`/`competition`, unlike `milestone`), so its payload-sufficiency fixture needs no `seed_sql`. Migration 0014 gave `goal` its first change-capture trigger. `goal.updated` deliberately not registered — no status-transition operation exists yet (`achieve`/`revise`/`retire` are real enum values with zero operations behind any of them), same non-speculation discipline as the three prior ADRs | `unit/kang/domain/projects/test_goal_service.py` (validation/payload) + `unit/kang/api/test_goal_operations.py` (handler-level) + `integration/sqlite/test_goal_store.py` (real SQLite ordering, horizon/status CHECK enforcement, change_log row) + `suites/replay/test_payload_sufficiency.py` (goal.created's payload-sufficiency fixture, EB-003/13 §16.2) |
| `task.complete` (added 2026-08-09) — the task entity's first status-transition operation: `open`/`scheduled`/`deferred` → `done`, `completed_at` stamped, `updated_at` stamped too (a real bug fix to `complete_task`, which predated this, its first real caller — the domain function only ever set `completed_at`, so a `task.updated` event announcing the transition would have carried the task's stale creation-time `updated_at` as its `occurred_at` instead of the actual completion time). Publishes the already-registered `task.updated` (ADR-004) under `kernel:tasks`, mirroring `deadline.updated`'s `_publish_deadline_alert` shape exactly (fetch, transition, publish, commit via the store's existing optimistic-concurrency `update()` — no new store-layer code needed, `TaskStore.update()`'s revision-conflict/not-found contract was already fully proven by the port-contract suite before this operation existed to drive it). Completing an already-done task is a typed domain error (`invalid_request`), not silently accepted; a genuine concurrent-write race surfaces as `conflict` with the current revision (API-006), proven with a monkeypatched store rather than asserted | `unit/kang/domain/tasks/test_task_service.py` (`complete_task`'s `updated_at` fix) + `unit/kang/api/test_task_operations.py` (handler-level: publishes task.updated recovery-grade, not-found/invalid-request/conflict error mapping, no double-publish on a rejected re-complete) |
| `serve()` runs the scheduler's boot catch-up (added 2026-08-09, D014 — "on startup after downtime, each job's policy decides") — `Scheduler.catch_up()` had been fully built and exhaustively tested (`test_morning_plan_job.py`, direct `build_core()` + `.catch_up()` calls) since M3, but nothing in the real boot path (`composition.py::serve()`, the function every actual launch of KANG runs) ever called it — confirmed by direct code read, not assumed. This is the smallest real fix, deliberately scoped: one call, before the operation channel accepts requests, no new mechanism. Explicitly NOT built here: a continuous tick loop for newly-due jobs while the process keeps running (needs a supervised-task primitive that doesn't exist anywhere in this codebase yet — 11_CODING §25 bans unsupervised threads — **closed 2026-08-10 by ADR-019, see below**), and wiring `deadline.sweep` as a second real job (would need a new `kernel:scheduler` scope grant, an authority-path change requiring its own ADR — **closed 2026-08-11 by ADR-020, see below**) — both real, named gaps, not silently folded into "scheduler now runs" | `suites/replay/test_boot_catchup.py` (real subprocess `serve()` boots: a backdated job's missed slot is caught up exactly once before the session file appears; a freshly-registered job with nothing missed boots clean with zero runs; a missing `kang.toml` — `core.scheduler is None` — boots clean with automation off, not bricked) |
| `milestone.reach`/`.miss`/`.drop` (added 2026-08-09, ADR-018) — the milestone entity's first status transitions, `pending -> {reached\|missed\|dropped}`. Unlike `task.complete` (whose store/domain layers already existed, waiting), none of this existed: `MilestoneStore` gained `get`/`update` (optimistic-concurrency contract identical to `TaskStore`'s), the domain service gained `mark_reached`/`mark_missed`/`mark_dropped` (mirroring `deadline_service.py`'s `mark_alerted`/`mark_met`/`mark_missed` exact shape), and `milestone.updated` was newly registered (ADR-018, the standing `.updated` pattern generalizing ADR-016's `.created` precedent). The three API handlers share one internal factory (`_make_milestone_transition_handler`) parameterized by which domain function to call — not three near-duplicate handlers. A stale read surfaces as `conflict` with the current revision, same as `task.complete` | `unit/kang/domain/projects/test_milestone_service.py` (all three transitions, non-pending rejection) + `unit/kang/api/test_milestone_operations.py` (handler-level: publishes milestone.updated, not-found/invalid-request mapping) + `integration/sqlite/test_milestone_store.py` (real SQLite `get`/`update`, revision-conflict, change_log row naming `status` as the changed field) + `suites/replay/test_payload_sufficiency.py` (milestone.updated's fixture, seeded parent project like its `.created` sibling) |
| `goal.achieve`/`.revise`/`.retire` (added 2026-08-09, ADR-018) — the goal entity's first status transitions, `active -> {achieved\|revised\|retired}`, same shape as milestone's own transitions above: `GoalStore` gained `get`/`update`, the domain service gained `achieve_goal`/`revise_goal`/`retire_goal`, `goal.updated` was newly registered. The three API handlers share one internal factory (`_make_goal_transition_handler`). `_build_handlers` (composition.py) crossed the size lint's 80-line hard limit the moment this landed — split into `_build_project_cluster_handlers` (project/competition/milestone/goal, the four entities `domain/projects/`'s own package grouping already treats as one cluster), not a relaxed limit | `unit/kang/domain/projects/test_goal_service.py` (all three transitions, non-active rejection) + `unit/kang/api/test_goal_operations.py` (handler-level: publishes goal.updated, not-found/invalid-request mapping) + `integration/sqlite/test_goal_store.py` (real SQLite `get`/`update`, revision-conflict, change_log row) + `suites/replay/test_payload_sufficiency.py` (goal.updated's fixture) |
| `project.complete` (added 2026-08-09, ADR-018) — the project entity's only status transition this pass, `active -> completed`. Unlike milestone/goal (which got their full named verb sets — see ADR-018's own scope ruling), `project.pause`/`.resume`/`.archive`/`.abandon` stay deliberately unbuilt: no prior document names a specific project verb set, and building all five with no named consumer beyond "the enum allows it" would be the speculative-structure anti-pattern this project rejects everywhere else. Same store/domain/API shape as `task.complete`/milestone/goal — `ProjectStore` gained `get`/`update`, `complete_project` mirrors `complete_task`'s exact shape, `project.updated` was newly registered (already landed in the milestone commit's shared ADR-018 event-registry batch) | `unit/kang/domain/projects/test_project_service.py` (completion + non-active rejection) + `unit/kang/api/test_project_operations.py` (handler-level: publishes project.updated, not-found/invalid-request mapping) + `integration/sqlite/test_project_store.py` (real SQLite `get`/`update`, revision-conflict) + `suites/replay/test_payload_sufficiency.py` (project.updated's fixture) |
| The live scheduler tick loop (added 2026-08-10, ADR-019) — closes the gap the boot-catch-up claim above named: `Scheduler.tick()` (a thin wrapper around the already-idempotent `catch_up()` that catches and audits, as `automation.tick_failed`, any failure `catch_up()` itself doesn't already isolate per-job) is re-run every `TICK_INTERVAL_S` (60s, a plain constant) from `service_actions()` — the stdlib hook `serve_forever`'s poll loop already calls once per cycle, on the exact thread that owns the thread-confined `kang.db` connection (DB-001). No new thread, no new connection, no new primitive: `http_binding.py::make_server` gained one `server_class` parameter and stayed fully scheduler-ignorant; the composition root builds the ticking subclass, timed via the injected `Clock` (never wall time — 11 §25). Live-verified against a real, continuously-running (never restarted) throwaway Core: a job made due only after boot was picked up and executed (`outcome='ok'`) by the live tick, not by any subsequent boot — see the ADR's own "Live verification" section for the full trace | `unit/kang/kernel/scheduler/test_scheduler.py` (`tick()`'s happy path + failure containment, audited as `automation.tick_failed`, never escapes) + `unit/kang/kernel/runtime/test_scheduler_tick.py` (the `service_actions()` gating logic: first call ticks immediately, no re-tick inside the interval, re-ticks once elapsed, a `None` scheduler is a silent no-op) + `unit/kang/api/test_http_binding.py::test_make_server_honors_a_custom_server_class` |
| `deadline_sweep` wired as an automatic job (added 2026-08-11, ADR-020) — closes the other gap the boot-catch-up claim above named: `kernel:scheduler`'s grant gains `deadlines.mark_alerted` (`permissions.toml`, the sole authority-path change this ADR exists to record), a second `Job` (`id="deadline_sweep"`, `schedule="hourly"`, `catch_up="run_once_latest"`, per 05_AGENTS Appendix E) is registered in `_wire_scheduler` alongside `morning_plan`, and `JOB_OPERATIONS` maps it to `deadline.sweep`. No new scheduler mechanism — `_catch_up_job`/`Scheduler.tick()` (ADR-019) already iterate every registered job, so a second job is handled for free; this is what proves ADR-006's job→operation mechanism actually generalizes past its first instance, not just claims to | `suites/replay/test_boot_catchup.py::test_deadline_sweep_is_registered_and_boot_catches_up_a_missed_hour` (real subprocess `serve()` boot: a `deadline_sweep` job backdated 3 hours is caught up exactly once, `outcome='ok'` — proving the `deadlines.mark_alerted` grant is real, not just that a row exists — while `morning_plan`'s own catch-up count stays independently at zero) |
| `job.timeout_s`'s soft overrun signal (added 2026-08-12, D014 — "jobs execute as supervised tasks: timeout... health status on the dashboard") — a real design conversation concluded hard enforcement is not ripe: Python cannot force-kill a thread, Windows has no `SIGALRM`, and both currently-wired jobs (`plan.generate`, `deadline.sweep`) are confirmed-by-reading zero-model-call pure SQL with nothing that could hang in the way AG-007's cooperative-cancellation model was designed for. What shipped instead is honest about that limit: `Scheduler._record_overrun_if_any` compares elapsed time (via the injected `Clock`, never wall time — 11 §25) around the already-blocking runner call and audits `job.overrun` — a post-hoc report, never enforcement, never affecting the run's actual outcome. `deadline_sweep`'s job row gets an explicit `timeout_s=120` (05_AGENTS Appendix A names it directly); `morning_plan` stays at the schema default (300s) — the planner agent's "10m" in Appendix A is a budget across three different job triggers, not morning_plan's own number, so it was deliberately not reused to avoid inventing a figure nothing actually names | `unit/kang/kernel/scheduler/test_scheduler.py` (a run past its timeout is audited but not stopped; a run within budget is not audited; a run that both fails AND overruns surfaces both signals independently) + live-verified against a real, continuously-running (never restarted) throwaway Core: `deadline_sweep` backdated with `timeout_s` patched to 0 mid-session, the live tick (ADR-019) picked it up for real and produced a real `job.overrun` audit entry (`elapsed_s=0.052`) while the run itself still completed `outcome='ok'` |
| `job.enable`/`job.disable`: the first real consequential operation, built end to end (added 2026-08-13, ADR-021) — closes the gap the 2026-08-05 held_action claim above named. `job.disable`/`job.enable` (already named in 12_API.md:182, not a new decision) never perform their effect directly — each always creates a `HeldAction` (now carrying `params`, the schema delta ADR-001 called "owed", finally applied — migration 0015) and raises `confirmation_required` via a new shared gate helper, `require_confirmation` (`consequential.py`, ADR-021's first callers). `held_action.approve`'s handler was rewritten: for `commit_mode="transactional"` (resolved from the *target* operation's own registry entry, not `held_action.approve`'s own), it drives the approve-flip, the target's effect (`TRANSACTIONAL_EFFECTS`, composition.py — the same plain-literal shape `JOB_OPERATIONS` already established), and `mark_executed` inside ONE real `BEGIN`/`COMMIT` — the first actual implementation of ADR-001 Amendment's "a crash before commit is indistinguishable from still-pending" promise, via new `_in_txn` store-method variants (`HeldActionStore.approve_in_txn`/`mark_executed_in_txn`, `JobStore.set_enabled_in_txn`) that assume the caller already owns the transaction. `redrive` mode (`calendar.write`) is untouched — falls back to the pre-ADR-021 flip-only behavior | `unit/kang/api/test_job_operations.py` (the gate: always raises `confirmation_required`, never disables/enables directly, validates `job_id`/`reason`) + `integration/sqlite/test_held_action_transactional_effect.py` (real SQLite: approve drives the effect to `executed` in one transaction; a failing effect rolls back BOTH the held action's flip and the target write — proven by forcing a real exception mid-sequence, not asserted; a missing effect function is a loud `internal` error, never a silent flip; an expired action's rollback leaves both rows untouched) + `integration/sqlite/test_held_action_gate_to_executed.py` (the full real `Dispatcher`: gate → approve → executed → the job is genuinely disabled/re-enabled, plus a plugin session denied at the scope check before ever reaching the gate) + `fixtures/held_action_store_contract.py`/`fixtures/job_store_contract.py` (the new `_in_txn` methods' guard contracts, fake + sqlite identically) |
| **C1-style crash-kill gate for `held_action.approve`'s transactional driver** (added 2026-08-13, ADR-021 Amendment — found missing by external cross-review, not self-discovered) — the original evidence for "one real transaction" was a happy-path HTTP trace plus an in-process Python-exception rollback test; neither is a genuine OS-level kill mid-transaction, and the existing C2 crash-replay suite covers the event bus's EB-004 write order exclusively, a different mechanism this path doesn't use. Closed same-day, mirroring `paired_write_worker.py`'s exact established pattern (a decorator around the real `SqliteHeldActionStore`, `os._exit(9)` at real boundaries, no test seam in production code): five real subprocess kills, one per boundary inside `_approve_transactional`'s `BEGIN IMMEDIATE`/`COMMIT` window. The hardest case named by the review — "the effect succeeds but mark-executed fails" — is proven directly: the job's own row is written on the connection, one write short of a fully committed effect, and a real process kill there still leaves the held action `pending` and the job row completely untouched on reopen | `fixtures/transactional_approve_worker.py` + `suites/replay/test_transactional_approve_crash.py` (`before_approve`/`after_approve`/`after_effect`/`after_mark_executed` all converge to fully-untouched-and-pending on real process kill; a clean run commits everything; `PRAGMA integrity_check` asserted on every recovery) |
| `held_action.expire` wired as a third automatic job (added 2026-08-14, ADR-022) — closes a real bug found in review: `HeldActionStore.expire_due()` had existed since M3 with zero callers anywhere in `src/` (confirmed by grep, not assumed — the only hits were the method's own two adapter implementations and the domain port's protocol). Unlike `deadline.sweep` (which already existed as a manual operation before ADR-020 wired it as a job), `expire_due()` had no operation wrapping it at all — this ADR builds one (`held_action.expire`, same shape as `deadline.sweep`'s, new scope `held_actions.expire`, no event publication since no `held_action.*` event type is registered anywhere and inventing one would repeat ADR-021's own declined "enum allows it" anti-pattern), registers a third `Job` (`schedule="daily"`, justified against the 24h expiry window rather than picked), and grants `kernel:scheduler` the new scope the same way ADR-020 added `deadlines.mark_alerted`. Landing this pushed `composition.py` past two hard size-lint limits at once — closed for real via ADR-023's module split (`scheduler_wiring.py`), not a same-file trim, extending the composition-root import exemption to a second file by exact name | `unit/kang/api/test_held_action_operations.py::TestExpire` (only past-pending actions move; a clean store returns count 0; running twice is idempotent — the second pass finds nothing left) + `suites/replay/test_boot_catchup.py::test_held_action_expire_is_registered_and_boot_catches_up_a_missed_day` (real subprocess boot: a real, genuinely-expired `pending` held action seeded directly in `kang.db` before boot genuinely transitions to `expired` after the job's boot catch-up runs — `expired` since ADR-024's terminal-state split, `cancelled` before it — not that a job_run row exists, that the actual store write happened; the other two jobs stay independently unaffected) |
| `commit_mode="transactional"`'s own registration-time gate (added 2026-08-14, closing an item found in review) — `redrive`'s registration-time idempotency-contract gate (`registry/__init__.py`, ADR 001 Amendment) has no `transactional` counterpart: an operation registered `transactional` with no matching `transactional_effects` entry previously failed only at *approval* time, inside `held_action_ops.py::_approve_transactional`'s own `internal` error. Mirroring redrive's gate verbatim into `registry/__init__.py` is impossible — the fact it needs (does an operation's declared mode have a matching effect?) doesn't exist until `transactional_effects` is built from live adapter closures in `composition.py`, and `kang.api.registry` may not import adapters or the composition root (17 §4.3.8). `_check_transactional_effects_registered` lives in `composition.py` instead, called from `_build_consequential_handlers` right after the table is built — the earliest point both halves are in scope together — so a missing effect now fails at boot, on every `build_core()` call, never only if/when someone happens to approve that specific held action. Caught during implementation, not assumed: `held_action.approve`/`.cancel` also declare `commit_mode="transactional"`, but for their own direct effect (the status flip), never as a lookup target — a naive "every transactional entry needs a key" gate false-positives on both and would have broken every real boot; excluded by construction (`HeldAction.operation` can never legitimately name an approval), recorded as `TRANSACTIONAL_SELF_EFFECT_OPERATIONS`, not a name-list convenience | `unit/kang/kernel/runtime/test_transactional_effects_gate.py` (the real, complete table passes; a missing entry raises `NotImplementedError` naming the operation; an empty table fails for every real target) |
| `held_action.status`'s `cancelled`/`expired` terminal-state split (added 2026-08-17, ADR-024) — closes a gap found in review: `cancel()` and `expire_due()` wrote the identical literal `'cancelled'`, deliberate when written (the sweep had no scheduled caller so the collapse cost nothing) but no longer accurate once ADR-022 wired the sweep as a real job — both events now genuinely occur with nothing distinguishing them. Migration `0016` rebuilds the table (SQLite can't `ALTER` a `CHECK` in place, same pattern `0005` used) to add `'expired'`; `expire_due()`'s write target changes from `'cancelled'` to `'expired'`, `cancel()`'s guard and target are unchanged. Confirmed against the real `%KANG_HOME%` database before migrating (read-only query, no lock taken): zero `held_action` rows exist, so historical rows are not reclassified — stated in the migration's own comment, not attempted, since there is no way to know retroactively which event produced a pre-existing `cancelled` row. Scoped to exactly this one change — transition provenance and event publication, originally investigated alongside this, are filed separately as ADR-025/026, not built here | `unit/kang/api/test_held_action_operations.py::TestExpire` (asserts `expired`, not `cancelled`) + `suites/replay/test_boot_catchup.py::test_held_action_expire_is_registered_and_boot_catches_up_a_missed_day` (real subprocess boot, real `kang.db` read confirms `expired`) |
| Held-action transition provenance — `decided_at`/`decided_by` (added 2026-08-17, ADR-025) — closes the strongest finding of the 2026-08-16 investigation, re-verified independently before implementation: no held-action transition was attributable in durable state. `reason` and `correlation_id` are both written once at creation and describe the REQUEST; the dispatcher's audit payloads carry no request params (`_record_start` logs only `{"trigger": ...}`, `_finish` logs `None`, and neither function even receives `request` — a structural non-availability); `Invocation` has no `params` field; no `held_action.*` event exists. "Who cancelled this, and when" had no answer. Migration `0017` adds two nullable columns, written by every transition OUT OF `pending` in the same UPDATE as the status (so a terminal status can never carry an unset decider). **`mark_executed` deliberately does not write them** — approved → executed is not a transition out of `pending`, so the approve step's provenance must survive it; that is the ADR's central design call, not an omission. `decided_by` is never hardcoded: handlers pass `context.principal`, which the dispatcher derives from the session, so the expiry job records `kernel:scheduler` *structurally* (the identity `scheduler_wiring.py` already mints its session under) while a manual sweep correctly records `kang`. NULL on a terminal row means "predates ADR-025", never "nobody decided" — stated in the migration comment because the two readings are indistinguishable from the data | `fixtures/held_action_store_contract.py` (run identically against fake + sqlite: a new row has no provenance; approve/cancel record who and when, durably on re-read; the sweep records `kernel:scheduler` and stamps only rows it actually expires; a refused past-expiry approve stamps nothing; **both** `mark_executed` and `mark_executed_in_txn` preserve the approve step's pair) + `unit/kang/api/test_held_action_operations.py::TestProvenance` (the handler seam: the same expire handler records `kernel:scheduler` under a scheduler session and `kang` under Kang's — proof the value is read from context, not assumed) |
| `scope=None` closed to three deliberate operations (added 2026-08-17, ADR-027) — found in the M7 read-only investigation: `Dispatcher._authorize` **skips** the permission engine entirely when an operation declares no scope (`api/dispatch.py:180-182`), so default-deny never runs for it. 13 of 39 operations were unscoped; 10 were reachable by any authenticated principal with no capability check at all. This was deliberate, with a written rationale ("system metadata about the Core itself, not a domain resource a domain-verb scope would fit") that held only while every session principal was fully trusted — exactly two exist today, `kang` (holding `*`) and `kernel:scheduler`. 05_AGENTS §8 adds `agent:{id}` at M7, and SEC-004 (`10_SECURITY.md:118`) permits "no code path exempt from scopes except Kang acting through the UI as `kang`". Nine operations gained scopes (`audit.read`, `invocations.read`, `explain.read` shared by all five `explain.*`, `permissions.read`, `system.read`, `notifications.ack`); no grant file changed, since `kang`'s `*` covers them all. Three stay unscoped **by decision, each with a reason in the registry**: `registry.get` (the contract a client must read to learn what scopes exist — gating it is circular) and `held_action.approve`/`.cancel`, which 05_AGENTS `:475` explicitly makes channel-gated not scope-gated ("the first-party channel check (not a permission scope — §8) is what stands in for that second layer") — adding scopes there would contradict a normative sentence | `unit/kang/api/registry/test_registry.py` (the unscoped set equals exactly the three documented operations, so the next accidental `scope=None` fails the suite; each system-metadata read carries its named scope; all five `explain.*` share one; and `kang` reaches every new scope through `*` while `agent:researcher` is refused all six — proving the checks are real, not vacuous) |
| `task.*` is the TODO domain and only that; `invocation` is the async-work resource (added 2026-08-17, ADR-029) — closes a LIVE doc/code contradiction found while sizing ADR-028's V1, not a future risk. ADR-004 registered `task.updated` as `category="domain"`, `recovery_grade=True` (a TODO mutation); 15_EVENT_BUS §6.1's Lifecycle row simultaneously specified the same type name as "(API long-running tasks)" with grade "others no". Since `validate_registration` refuses any publish whose `recovery_grade` disagrees with the registry ("the redo contract is the registry's, not the publisher's" — EB-003), M7 publishing `task.updated` for an agent run exactly as the Lifecycle row instructed would have been rejected at runtime by a guard working as designed. Resolved in favour of the built side: `task.*` keeps the TODO meaning (3 operations, 2 event types, a store, a port, 2 scopes, a migration — all shipped since M1), and API-007's prose renames to `invocation_id` / `invocation.updated` / `invocation.cancel` / "invocation resource" (12_API already conceded it: "task state persists as `invocation` rows"). No operation was renamed because none of the async ones exists yet — which is exactly why the fix was free now and expensive after M7's first registration. UI labels (09_UI §5.2's "task card") deliberately NOT renamed — a human-facing word 09_UI owns | `unit/kang/kernel/bus/test_event_registry.py` (`task.updated` is domain + recovery-grade, so re-specifying it as Lifecycle/non-recovery-grade fails the suite instead of surfacing as a publish rejection mid-M7; and nothing squats on `task.*` for execution machinery, while `invocation.updated` stays deliberately unregistered until something publishes it) |
| The daily backup job — `backup.snapshot` (added 2026-08-17, ADR-031) — **KANG had never taken an automated backup.** `adapters/sqlite/backup.py`'s `integrity_check`/`vacuum_into` shipped at M1, are covered by 5 tests plus the full C1 restore drill, and had ZERO callers in `src/`; the module header's own promise that "the scheduled daily job (02:30) arrives with the scheduler at M3" expired unnoticed when M3 shipped three jobs, none of them backup. Second confirmed expired milestone promise (ADR-030 found the first). Wired as the fourth job on ADR-020/022's pattern: new `BackupService` port + sqlite adapter + fake, `backup.snapshot` operation, scope `backups.write` granted to `kernel:scheduler`. Two spec corrections rather than silent deviation: Appendix E's `run_all_missed` becomes `run_once_latest` (N days down would otherwise produce N snapshots of the same current database under N different dates), and its "Sleeping" product state is recorded as unbuildable — `Job` has no such field and product state exists nowhere in code. **Honest limit, per 07 Part XII.3 ("a backup that hasn't been restore-tested is treated as nonexistent"): this gives snapshots that are taken, recorded and pruned — NOT backups. The `backup.verify` job that would earn that word is ADR-031 D3's first deferral** | `integration/sqlite/test_backup_service.py` (real databases: both kang.db AND eventlog snapshotted — the event log is not optional, since DB-001's durability pairing replays it for post-snapshot Tier-1 effects; the snapshot is genuinely openable with its schema intact; manifest line written; current-month audit copied when present and reported as None rather than invented when absent; first-of-month promoted once; a same-day repeat refused; retention keeps newest 30 daily / 12 monthly by name-order, never mtime; and a PAGE-corrupted database — deliberately not header-corrupted, since that fails at `open_connection` before the gate is reached — is refused with nothing written) + `unit/kang/api/test_backup_operations.py` (the handler is thin: injected clock not wall time, and a refusal is a loud `internal`, never a degraded success — 05_AGENTS App. A's "alert on any failure, no silent skip, ever") + `suites/replay/test_boot_catchup.py::test_backup_snapshot_is_registered_and_a_real_boot_takes_a_real_snapshot` (real subprocess boot leaves real openable snapshots of both databases on disk, one manifest line, and exactly ONE snapshot after two days of downtime — proving the run_once_latest correction) |
| The monthly restore test — `backup.verify` (added 2026-09-11, ADR-032) — closes 07 Part XII.3's own standard, left open by ADR-031: "a backup that hasn't been restore-tested is treated as nonexistent." Two more of Part XII.3's own terms had no referent in code, checked rather than assumed: **the "named-query suite"** — none of the four views §4.1 names is a real `CREATE VIEW`; only two have any implementation at all (`DeadlineStore.active()`/`TaskStore.plannable()`, already cited in their own docstrings as the `v_active_deadlines`/`v_today_tasks` read shapes), since the other two (`v_project_memory`/`v_contested_records`) belong to Memory, an unbuilt Phase 2 stub. **"±expected churn"** — no tolerance number for it exists anywhere in the constitution, so row counts (live vs. snapshot) are reported as data, never gated, rather than invented by feel (ADR-013's own warning against picking by analogy, applied here to picking a number). A third gap: 05_AGENTS Appendix E says "monthly" but the scheduler's interval dialect supports only `every:{s}`/`daily`/`hourly`/`minutely` — no `monthly` — so this is wired via the `cron:` dialect (`cron:0 3 1 * *`) already load-bearing for `morning_plan`'s own wall-clock trigger, needing no new config. `verify_latest` opens the LATEST daily snapshot via a genuinely read-only URI connection (never `open_connection()`, which would write `-wal`/`-shm` files onto an artifact that is supposed to stay exactly what `VACUUM INTO` produced), and — the operation's central design call — **a failed check is a normal returned record, never an exception**: `BackupError` is reserved for "no snapshot exists to verify" at all, a structurally different condition from "checked and it's broken," which is precisely the finding 05_AGENTS Appendix A demands surface ("alert on any failure — no silent skip, ever") rather than disappear inside a raise | `integration/sqlite/test_backup_service.py` (verify raises only with nothing to check; checks the lexically-latest of multiple snapshots; runs the real production `SqliteDeadlineStore`/`SqliteTaskStore` read shapes against the restored connection — not a hand-duplicated query of them — and reports both checked and not-built shapes by name; row counts reported through a genuine live/snapshot mismatch with the run still succeeding; a page-corrupted snapshot returns `integrity_ok=False` as a normal record, proven NOT to raise; manifest lines carry a `kind` field distinguishing `snapshot` from `verify` in the one shared file) + `unit/kang/api/test_backup_operations.py` (the handler seam: a failed check reaches the caller as data, never an `ApiError`; only "nothing to verify" raises; injected clock) + `suites/replay/test_boot_catchup.py::test_backup_verify_is_registered_and_a_real_boot_restore_tests_a_real_snapshot` (real subprocess boot: a real snapshot seeded via the real production adapter, the job backdated 40 days, boot catch-up restore-tests it for real — first proof in this suite that a `cron:`-scheduled job catches up correctly on any job other than `morning_plan`; the manifest's `verify` line names both real read shapes by name, not merely that a `job_run` row exists) |
| `system.health` exposes backup age + last restore-verification result (added 2026-09-12, ADR-033) — the two of 09_UI §12's four Health fields with a real source once `backups/manifest.jsonl` existed (ADR-031/032); index parity and the integrity-incident counter remain the same honestly-named gap the response's own docstring already carried. `BackupService.latest_status()` is a new, read-only, clock-free method reading the manifest's last line of each `kind` — `last_snapshot_at`/`last_verify_at` are raw ISO-8601 timestamps (not precomputed ages, consistent with every other timestamp this API serves, e.g. `task.created_at`), and `last_verify_ok` is `integrity_ok AND (not read_shape_errors)`, never `integrity_ok` alone — a clean integrity check with a broken read shape is still a failed restore-test by 07 Part XII.3's own standard. Two states of "nothing yet" are kept distinct rather than collapsed: no snapshot ever (all three `None`) vs. a snapshot with no verify yet (`last_verify_at`/`last_verify_ok` `None`, `last_snapshot_at` populated) | `integration/sqlite/test_backup_service.py` (fresh home is all-`None`; reflects the most recent of each kind independently; a clean verify reports `last_verify_ok=True`; a verify with a broken read shape — one column renamed on the snapshot only, isolating that failure from `_row_counts`/`integrity_check` staying clean — reports `last_verify_ok=False`, proving the AND logic at the adapter) + `unit/kang/api/test_system_operations.py` (the same AND logic proven end-to-end through the handler, not just the port, via an injected `FakeBackupService`) |
| The off-machine backup warning (added 2026-09-13, ADR-034, 07 Part XII.5) — "KANG's own duty ends at `backups/`; the health panel warns (weekly) if `%KANG_HOME%` shows no evidence of external backup." A Kang-configured `[backup] external_marker_path` in `kang.toml` (deliberately tolerant, unlike `load_planner_triggers`'s fail-closed loader — a missing/malformed `[backup]` section reads as "unconfigured," never a startup failure); `BackupService.external_backup_status()` reads the marker's real mtime and reports `stale` against a 7-day threshold — read directly from Part XII.5's own "(weekly)" word rather than inventing a second number. A new sixth scheduled job, `backup_offsite_check` (weekly, `run_once_latest`, new scope `backups.read` since — unlike its `backup.write` siblings — it writes no file), publishes `backup.offsite_stale` only when stale (silence is the healthy state) through a new parallel notifier enqueue handler at `attention` priority. Two doc gaps surfaced, not silently patched: 09_UI §12 lists four Health fields, not this fifth one; 05_AGENTS Appendix E itemizes no job for this mechanism at all | `unit/kang/adapters/config/test_backup_config.py` (the marker loader never raises — absent, malformed, or partial `[backup]` sections all read as `None`) + `integration/sqlite/test_backup_service.py` (a real marker file's real mtime, fresh vs. 7-days-stale, unconfigured-vs-configured-but-never-written both read as stale) + `unit/kang/domain/notifications/test_notifier.py` (the new enqueue handler queues at `attention`, carries the logical `{kind:"backup",id:"offsite"}` ref, ignores other event types) + `unit/kang/api/test_backup_operations.py` (the handler publishes `backup.offsite_stale` only when stale — nothing published on a fresh marker — with `causation_id=None` since it is a genuine root cause) + `suites/replay/test_boot_catchup.py::test_backup_offsite_check_is_registered_and_a_real_boot_warns_when_stale` (real subprocess boot, backdated 10 days, against the shipped *unconfigured* default `kang.toml`: catches up exactly once and a real `attention` notification row lands in the real database) |
| The kernel's supervised-task primitive (added 2026-09-13, ADR-036 Slice 0, 11_CODING §12) — "all concurrency passes through the kernel's supervised-task primitives (timeout, cancellation, naming) — bare `create_task` outside the kernel is lint-banned." `create_supervised_task` is the first and only sanctioned way to spawn an `asyncio.Task` in this codebase: `name` is mandatory (an anonymous task is unanswerable per SEC-005), an optional `timeout_s` wraps the coroutine in `asyncio.wait_for` for genuine cancellation rather than the post-hoc reporting `Job.timeout_s` gives today (ADR-028 C1's own finding), and a done-callback logs any unhandled exception loudly (DB-P7) — a deliberately cancelled task is not logged as a failure, but a timeout is (its `TimeoutError` surfaces as the task's own exception, not as cancellation). Retry-with-backoff is deliberately NOT included — its own RESERVED item (03_ROADMAP §8), not ripe. Zero callers yet; the scheduler's own tick task (ADR-036 Slice 2) is the first real one | `unit/kang/kernel/runtime/test_supervised_task.py` (a nameless task refuses to start; a slow task is genuinely cancelled by its timeout, not merely flagged; both a timeout and an unhandled exception are logged with the task's own name in the message; a deliberately cancelled task is provably NOT logged) + `tools/lint_banned_patterns.py`'s new rule (a bare `asyncio.create_task` outside `kernel/runtime/` is caught; the real `src/` tree, including this primitive's own sanctioned use, passes clean) |
| DB-001's write-executor + read-pool (added 2026-09-13, ADR-036 Slice 1/D3) — "exactly one write connection, owned by a single async write-executor task... a pool (default 4) of read-only connections." `WriteExecutor`/`ReadPool` are built on `concurrent.futures.ThreadPoolExecutor` — the same mechanism `asyncio.to_thread()` itself uses (`loop.run_in_executor`), but with a purpose-built, fixed-size pool instead of the ambient shared one, since `sqlite3`'s `check_same_thread=True` requires the *same* thread across every call, not merely "a" thread — the exact trap a naive `to_thread()`-per-call design would have hit intermittently. `ReadPool` uses `ThreadPoolExecutor`'s `initializer` hook so each worker opens and permanently owns exactly one `query_only=ON` connection (a new `open_read_only_connection`, verified not just set, alongside `connection.py`'s existing `open_connection`). Zero callers yet — the `aiohttp`/command-query routing seam (ADR-036 D4) is the first real one | `integration/sqlite/test_connection_pool.py` (writes submitted concurrently via `asyncio.gather` still run in strict arrival order, proving the FIFO queue; an exception inside a submitted callable propagates to the awaiting caller; concurrent read submissions genuinely land on more than one connection — four 50ms jobs complete in well under their serial sum, not merely the appearance of a pool; a write attempted through a read connection raises `sqlite3.OperationalError`; a row the write executor commits is visible through the read pool) |
| The Core runs on `aiohttp` via one dedicated write-executor worker thread (added 2026-09-14, ADR-036 D4, closing ADR-035's own destination) — `http.server` is retired; `build_core()` is called *from* `WriteExecutor`'s one worker (reusing Slice 1's `WriteExecutor` unchanged — its `connect` factory was already general enough to own a whole `Core`, not just a bare connection) instead of from `serve()`'s own thread, so every store's connection is opened on, and forever used from, that same thread — `check_same_thread=True` satisfied by construction, not relaxed, and `build_core()` itself needed zero changes. Every dispatch, the boot catch-up, session minting, and shutdown all route through the same executor; `Dispatcher` itself needed zero changes too (the entire existing synchronous `dispatch()` is submitted whole, not given a new async entry point). The scheduler's live tick (ADR-019) is now a native `asyncio` loop (`_tick_forever`, a supervised task) instead of an `HTTPServer.service_actions()` override — no clock-based gating needed, `asyncio.sleep()` IS the interval. Query-kind operations still route through the write-executor today, same as commands — DB-001's read-pool concurrency benefit is not yet realized; that is its own deferred, precisely-scoped slice (ADR-036's own recorded findings on why splitting query dispatch across both executors needs its own care) | `unit/kang/api/test_http_binding.py` (7 tests: CORS via middleware reaches `aiohttp`'s own 404 for unregistered paths, not just hand-written routes; OPTIONS preflight; a malformed/empty body is `invalid_request`, never a crash) + `unit/kang/kernel/runtime/test_scheduler_tick.py` (4 tests: the tick fires repeatedly at the configured interval via the write-executor, not once; a silent no-op with no scheduler wired; clean cancellation) — plus the **entire pre-existing real-subprocess replay suite** (`test_boot_catchup.py`, `test_single_instance.py`, every other test spawning `python -m kang.kernel.runtime.composition`) passing **unchanged** against the new `serve()`, proving the external contract (`session.json`'s shape, boot-catch-up-before-first-request ordering, `AlreadyRunningError`'s one-line-stderr-and-exit-1 path) held exactly |
| 16 of 17 query operations genuinely run on DB-001's read pool, not the write-executor (added 2026-09-14, ADR-036 D4 resumed/completed, ADR-037) — `Dispatcher` gained four new phase methods (`prepare_query`/`run_query_handler`/`finish_query`/`fail_query`, plus `error_envelope`/`query_handler_names`), each a plain synchronous method; `dispatch()` itself is completely unchanged in signature and behavior — a new caller (`query_routing._dispatch_query`) threads the phases across `WriteExecutor`/`ReadPool` instead. A query dispatch is three hops: write-executor (auth/validate/checks/`_record_start`), read pool (the handler, built FRESH per call via a `Callable[[connection], Handler]` factory — never a boot-time instance bound to the write connection, which would use only one of the four pool workers regardless of concurrent load), write-executor (`_finish` + response). `system.health` is the one named, scoped exception — its `SqliteBackupService` dependency needs both `kang.db` and `events/eventlog.db`; `ReadPool` opens one connection type per worker — and keeps `dispatch()`'s original single write-executor submission. The audit hash-chain race named at the first D4 attempt stays discharged permanently, not just "for now": every audit WRITE (`_record_start`/`_finish`) still only ever runs on the write-executor's one thread, even for queries; audit READS (`JsonlAuditLog.records`/`chain_head`, what `audit.list`/`explain.invocation` call) open their own file handle per call with no shared mutable state, safe under concurrent read-pool callers by inspection, not assumption. `query_routing.py` is a new file carrying the composition-root import exemption (ADR-037, the same pattern ADR-023 established for `scheduler_wiring.py`) — split out when this slice pushed `composition.py` past the size lint's hard limits | `unit/kang/api/test_dispatch.py` (14 new tests: `query_handler_names` reflects the table; `prepare_query` authenticates-before-registered exactly like `_run`, records the invocation+audit 'dispatched' entries; `run_query_handler` builds fresh against whatever connection it's given, never a captured write-bound one; `finish_query`/`fail_query` mark the invocation ok/failed and record the matching audit entry; `error_envelope` wraps an `ApiError` verbatim and an unexpected exception as `internal`; the phased path and `dispatch()`'s own single-call path produce the identical response shape for an equivalent query) + `unit/kang/kernel/runtime/test_query_routing.py` (5 tests: `_build_query_handlers` covers exactly the 16 expected operation names and excludes `system.health`; every factory builds a callable Handler without touching a real DB; `_dispatch_query`'s own sequencing — prepare and finish run on the write-executor, the handler on the read pool, in that order; a handler `ApiError` triggers `fail_query` then `error_envelope`, never `finish_query`; a `prepare_query` failure goes straight to `error_envelope`, skipping the read pool and `fail_query` entirely) — plus the full 920-test suite (unchanged from before this slice) and the entire real-subprocess replay suite passing with zero changes, proving `dispatch()`'s own external contract held exactly |
| Core-side single-instance enforcement (added 2026-08-10, ADR-008 Part A2 — the deferred half of an already-accepted decision, not a new design call) — `build_core()` takes an exclusive OS-level lock (`FileStartupLock`, `msvcrt.locking()` on `%KANG_HOME%/core.lock`) before opening `kang.db` or the eventlog; a second live Core against the same home gets `AlreadyRunningError` immediately, nothing else touched. `serve()` reports a one-line stderr message and exits 1 rather than crashing with a traceback or racing a second scheduler/notifier against the same truth. The OS releases the lock on any process exit, clean or crashed — no PID-file staleness window to get wrong. Windows-only code (`msvcrt`) confined to `adapters/os_windows/` behind the `StartupLock` port, satisfying NFR-010 ("no Windows-only **core** dependency") at the port boundary rather than by the adapter being portable itself | `tests/fixtures/startup_lock_contract.py` (shared port-contract suite: acquire/release round-trips, a second acquire while held raises, release lets a pending acquire through, release is safe unconditionally and idempotent) run identically against `unit/kang/adapters/fakes/test_startup_lock_fake.py` and `integration/os_windows/test_startup_lock.py` (the real adapter — first occupant of that integration directory) + `suites/replay/test_single_instance.py` (real subprocess proof: two genuine `python -m kang.kernel.runtime.composition` processes against the same real `%KANG_HOME%` — the second exits 1 with a stderr message naming the lock, the first's session handshake is byte-for-byte untouched; after the first stops, a third process starts clean with no manual cleanup) |
| The Model Router routes a `TaskSpec` to a provider without the caller ever naming a model (added 2026-09-14, D010, ADR-038 D1-D5, first M7 slice) — "callers declare intent, not models... the router maps spec → provider via `providers.toml`." `TaskSpec.privacy_tier` reuses `sensitivity`'s exact three-value vocabulary (06_MEMORY, 07_DATABASE §5.1), orthogonal to `task_class` — a `routine`-class call can still carry `privacy_tier="private"` content. `Router.route()` tries `providers.toml`'s chain for a task class in order: `ProviderUnavailable` advances to the next candidate (logged `fallback`, or `error` if it was the last); `ProviderRefused` propagates immediately, never advancing the chain (a caller bug, not a fallback case); a per-provider circuit breaker (consecutive-failure threshold + cooldown, both `providers.toml`-configured, injected `Clock` for deterministic testing) skips a recently-failed provider without retrying it. `privacy_tier="private"` fails closed by construction — the candidate chain is filtered to `local_only` entries only, and with no Ollama adapter built yet (Phase 5, RESERVED) that filtered chain is empty for every task class today, so a private call gets `NoProviderAvailable`, never a silent cloud fallback. Every attempt, success or failure, is logged to a new `model_call` table (migration 0018 — AG-008's ledger half, "every call lands in `model_call`"); AG-008's *threshold enforcement* half (80%/95%/100% degradation) is explicitly NOT implemented — `providers.toml`'s `[budget]` section is parsed and carried (`ProvidersConfig.monthly_cap_usd`/`per_task_class_cap_usd_by_class`) but unread by the Router, a named gap (RESERVED, trigger: a real caller exists to generate real spend to threshold against), not a silent partial implementation. `providers.toml` fails closed like `permissions.toml` (an absent/invalid file yields empty chains for every task class), not open like `kang.toml`. ADR-038 D1's own scope cut: only the `ModelProvider` port, its fake, the Router, and the config loader ship this slice — the real Anthropic adapter needs a credential subsystem that does not exist anywhere in this codebase yet (zero code reads the OS keychain today), named as its own next slice rather than folded in; the `anthropic` SDK is declared as a dependency now regardless (ADR-038 D6), unused until that slice lands, the same "declare ahead of the code" shape `aiohttp`/ADR-035 used | `unit/kang/kernel/router/test_router.py` (11 tests: single-provider routing; a `ProviderUnavailable` genuinely falls over to the next provider, both tried; `ProviderRefused` propagates without ever trying the second provider; every candidate failing propagates the LAST provider's own error, not a synthesized one; an empty/unfiltered chain raises `NoProviderAvailable` with nothing logged at all; `privacy_tier="private"` fails closed with the cloud provider never even called, and routes correctly when a `local_only` provider IS configured; a `providers.toml` entry naming a provider not wired at the composition root is skipped, not a crash; the circuit breaker opens at the configured threshold, stays open through the cooldown window via `FakeClock.advance()` — no real sleep — and closes again after it elapses; a success resets the consecutive-failure count so an isolated later failure alone never trips the breaker) + `unit/kang/adapters/config/test_providers_loader.py` (10 tests: chain/local_only/circuit-breaker/budget parsing; malformed TOML and a missing `model`/`name` field both raise; an absent file raises for the caller's own fail-closed fallback; the shipped default loads, routes `routine` to `anthropic`, and has an empty `private` chain) + `integration/sqlite/test_model_call_store.py` (4 tests: a row persists with every field intact; multiple calls accumulate as separate rows; rows survive a reopen; the CHECK constraint rejects an outcome outside the closed four-value set) — plus a live end-to-end check against a real throwaway `%KANG_HOME%` (real migration 0018 application, the shipped `providers.toml` loaded for real, a real `SqliteModelCallStore` row written and read back) and the full suite (961 passed, up from 935) |
| The credential subsystem is real, and the Model Router can reach a real Anthropic response (added 2026-09-14, SEC-011, ADR-039) — "raw secrets exist in exactly one place (OS keychain)... adapters request credentials by name at call time; credentials live in memory only for the duration of the call." `CredentialStore` is deliberately `get`-only — no `set`/`delete` on the port at all: rotation is Kang's own action ("in provider consoles + keychain update"), never KANG's; Kang sets up a credential via `keyring`'s own CLI (ships with the dependency) or the native Credential Manager UI. `KeyringCredentialStore` wraps `keyring` rather than hand-rolled `ctypes`/`win32cred` (reinvents a solved, security-sensitive problem — the third time this project has declined that trade, after ADR-011/ADR-035) or `pywin32` directly (unnecessarily broad — `keyring`'s own Windows backend pulls in less than `pywin32` would). `AnthropicProvider` implements ADR-038's `ModelProvider` port for real: the API key is fetched fresh from `CredentialStore` on every call and used to construct a genuinely new SDK client each time — never cached on the instance, matching SEC-011's "in memory only for the duration of the call" literally. The SDK's own exception hierarchy maps onto the port's two fallback-relevant errors: request/auth/permission/not-found/conflict/unprocessable/too-large errors → `ProviderRefused` (never retried, never advances the Router's fallback chain — an `AuthenticationError` on one provider isn't fixed by trying a different one); every other `AnthropicError`, including subtypes this adapter has never seen, → `ProviderUnavailable` (fallback-eligible, the safer default for an unrecognized failure); a missing/misconfigured credential ALSO becomes `ProviderUnavailable` (found during implementation, not pre-specified: a provider with no working credential is the same shape as a provider being unreachable, so the Router's own fallback chain should get to try elsewhere rather than the whole route() call crashing on an exception type it doesn't declare). Structured output (`response_schema`) is explicitly NOT implemented — raises `NotImplementedError` loudly rather than silently ignoring the request; nothing calls this adapter with a schema yet (the Router still isn't wired into `Core` — ADR-038 D1's own scope cut, unchanged by this slice). Cost is computed from a small adapter-owned `$/token` table, explicitly a snapshot that will drift as Anthropic's real pricing changes; an unrecognized model id yields `cost_usd=0.0` as a flagged UNPRICED case, never a guessed number. A real gap found in the already-accepted ADR-038 while grounding this one: `ModelProvider.call()` never actually carried which concrete model to call, even though `providers.toml`'s `ProviderEntry.model` already names one per chain entry — fixed at the source (the port, `Router.route()`'s call site, `FakeModelProvider`, and the one affected test), not worked around in the new adapter | `tests/fixtures/credential_store_contract.py` (a shared port contract, run identically against both implementations — 13 §2.3's fake/real pairing discipline) against `FakeCredentialStore` (3 tests) AND `KeyringCredentialStore` (3 + 1 backend-resolution check) — the latter against the REAL Windows Credential Manager: every credential it writes is `kang-contract-test-`-prefixed and deleted in its own teardown, confirmed left behind nothing by direct post-run inspection, not assumed from the teardown code; `CredentialNotFound`'s message asserted to never contain a real seeded value, even when one exists nearby in the same test. `unit/kang/adapters/anthropic/test_provider.py` (9 tests, mocking only `anthropic.Anthropic` itself — every response/exception object is a REAL SDK type, `anthropic.types.Message`/`Usage`/`TextBlock`, real `BadRequestError`/`AuthenticationError`/`APIConnectionError` instances, so a field-name typo here fails the same way it would against the real SDK): a successful call's text/tokens/cost extracted correctly; the credential fetched fresh every call via two genuinely separate client constructions, never one held across calls; a missing credential raises `ProviderUnavailable` before any client is even constructed; `BadRequestError`/`AuthenticationError` both map to `ProviderRefused`; `APIConnectionError` maps to `ProviderUnavailable`; an unrecognized `AnthropicError` subtype still defaults to `ProviderUnavailable`; an unrecognized model's cost is provably `0.0`; `response_schema` raises `NotImplementedError` — plus the full suite (977 passed, up from 961) and zero live network calls anywhere in it (13_TESTING §1) |
| The agent registry is real: all 15 of Appendix A's catalog agents load, validate, and pairing-lint as data — most still unrun, `deadline_sweep` genuinely runnable (added 2026-09-14, completed 2026-09-16, AG-004, ADR-040/ADR-041) — "Agents exist only as registered definitions: versioned declarative documents... loaded at startup... KANG MUST NOT synthesize new agents, modify definitions, or grant scopes at runtime." A genuine scoping finding made before any code: "manifest assembly" (AG-009's Context Assembler) is NOT M7 work — `kernel/context/__init__.py` already states its own constitutional home as Phase 2, blocked on Memory's write gate/scoring, neither of which exists yet; building it now would be IM-003's illegal parallelization. This ADR is the registry only. `agents/definitions/{id}/{id}.toml` + `prompts/` (17_PROJECT_STRUCTURE's own already-named layout), parsed into a typed `AgentDefinition` by `adapters/config/agent_definitions_loader.py` (structural validation only: required fields, the two-value `kind` enum, positive `timeout_s`, non-negative `retry`, non-empty `degradation`, `tools`' own AG-005 rules — the key must be present even when the value is an empty tuple, no empty-string or duplicate entries — `prompt_file`/`escalation` shape rules tied to `kind`). AG-005's tool allowlist and §8's capability scopes are two different fields with two different grammars, not one list read two ways: `scopes` reuses the EXISTING `kernel/permissions/pairing.py::lint_grants`/`parse_scope` verbatim (composed in `kernel/orchestrator/registry.py::build_checked_registry`, since adapters may not import kernel — the same parse-in-adapters/lint-in-kernel split `permissions_loader.py`/`build_checked_engine` already established for grants) — a bad agent *design* is now caught at definition time, not only once someone adds it to `permissions.toml` for real; `tools` gets structural validation only, since AG-005 itself defers what a tool string resolves to onto a future executor slice not yet decided. §1.2's `mechanical agent MAY escalate to a cognitive step` shape (Appendix A's own `M→C` rows: `competition_scout`, `web_monitor`, `memory_steward`) is modeled as an explicit `EscalationCapability(task_class, prompt_file)` — forbidden on cognitive definitions, optional on mechanical ones, `task_class` reusing `TaskSpec`'s own enum (ADR-038 D2) rather than inventing a second one. Registry loading fails closed like `permissions.toml`/`providers.toml`: any single malformed definition refuses the WHOLE load, never a partial registry smaller than the one on disk. Shipped 3 of the 15 catalog agents at first (`deadline_sweep` mechanical/zero-escalation, `critic` cognitive/zero-tools — the "ALL world-touching tools forbidden" edge case, `planner` cognitive with a real pipeline membership and the most load-bearing scope set), chosen for schema coverage — the same "smallest correct slice" discipline ADR-036/038/039 D1 already used — then the remaining 12 (2026-09-16, ADR-040 D4 completed): `competition_strategist`, `competition_scout`+escalation, `researcher`, `tutor`, `memory_steward`+escalation, `vault_indexer`, `vault_organizer`, `web_monitor`+escalation, `notifier`, `backup_monitor`, `health_monitor`, `faith_companion` — all 15 load and pairing-lint cleanly. `sync_agent` (reserved, undefined until 16_SYNC exists) and `plugin_runner` (a per-plugin template, not a static catalog entry) stay excluded. Two of the twelve (`backup_monitor`, `health_monitor`) got `tools` resolved to real registered operations — their operations already exist and already perform the whole mandate, ADR-041's own shape; the rest stay illustrative like `critic`/`planner` already were, since no cognitive executor exists yet to make resolving them urgent — `notifier` named explicitly as a case that may never fit the operation-dispatch model at all (its real mechanism is a bus subscriber, not an invocable thing). A design correction found while implementing the first 3 (not pre-specified): the ADR's own draft named "duplicate id across two folders" as an expected proof point, but D3's own folder/id-match rule makes that structurally impossible under a flat layout — the dead check (and its untestable test) were removed rather than kept for appearances | `unit/kang/adapters/config/test_agent_definitions_loader.py` (27 tests: every required field's absence; the `kind` enum; non-positive/non-integer `timeout_s`; negative `retry`; `tools`' three rules including that an empty tuple IS valid; a cognitive definition missing or pointing at a nonexistent `prompt_file`; a mechanical definition setting one anyway; `escalation` forbidden on cognitive, valid on mechanical; `escalation.task_class` outside `TASK_CLASSES`; a missing `escalation.prompt_file`; a folder/id mismatch; `discover`'s folder-walk and missing-manifest cases; all 15 real shipped definitions loading with their exact expected shapes, including both resolved-tools cases and both escalations' task classes) + `unit/kang/kernel/orchestrator/test_registry.py` (8 tests: a clean multi-agent registry builds/indexes correctly; each of the pairing lint's real rules refuses when violated by an agent's own scopes — `web.fetch`+`memory.read:sensitive`, `web.fetch`+`vault.write`, `memory.propose:rule`/`profile` ungrantable, wildcard forbidden for any agent principal; one bad agent among several refuses the WHOLE registry; the real 15-agent shipped catalog passes the lint end to end) — plus the full suite (1017 passed) and zero network/model calls anywhere in it (13_TESTING §1) |
| The mechanical-agent executor is real: a tool call is an operation dispatch, proven against a genuinely running `deadline_sweep` (added 2026-09-16, 05_AGENTS §3, ADR-041) — cognitive agents stay out by construction (`CognitiveAgentNotSupported`, refused before any dispatch), matching 03_ROADMAP's own Phase 1 scope ("all cognitive agents beyond basic chat" postponed). Resolves what "tool" means, which AG-005 itself left open: a tool call IS a session-minted operation dispatch through the SAME `Dispatcher.dispatch()` pipeline every UI command and scheduled job already uses — no new Tool port, no second permission model. `run_mechanical_agent` mints a session for principal `agent:{id}`, `first_party=False`, mirroring `scheduler_wiring.py::_make_job_runner`'s own already-accepted reasoning verbatim ("a job is not Kang's hand" applies to an agent identically — SEC-003 by construction, no agent principal can ever approve a held action). The allowlist check (`operation in agent.tools`) runs BEFORE any dispatch is attempted, independent of and prior to the dispatcher's own scope check (ADR-028 C4, already-settled). A real layering consequence found while implementing: `kang.agents` is unconditionally forbidden from importing `kang.api` (`tools/importlinter.toml`'s existing contract, not merely "pre-M7") — so `Dispatch` is a plain generic `Callable[[str, dict, str, str|None], dict]`, never `ApiRequest`/`Dispatcher` themselves, mirroring `api/http_binding.py`'s own "stays ignorant of HOW a request reaches the Core" pattern; `lint-imports` confirms all 8 contracts still kept with the executor in the tree. Corrects ADR-040's own `deadline_sweep.toml`: `tools` originally transcribed Appendix A's illustrative prose (`deadlines.read`, `deadlines.mark_alerted`, `notify:critical`) verbatim, none of which names a real, separately-callable thing — `deadline.sweep` already performs the whole mandate internally, and `deadlines.mark_alerted` was that operation's own required *scope*, not a tool name, sharing a string by coincidence. Corrected to `tools = ["deadline.sweep"]`. Four things deliberately NOT this slice, each named: no real `permissions.toml` grant for any agent principal (proven only against a throwaway config); not wired into the scheduler (the existing `deadline.sweep` job keeps running unchanged — this is a second, parallel, unwired path); no outer `kind="agent"` invocation row (`domain/ports/invocation.py`'s own "'agent' joins at M7" comment stays unactioned — a single-tool agent's underlying operation record is already a complete audit trail); no concurrency/budget enforcement (nothing tracks either yet) | `unit/kang/agents/runtime/test_executor.py` (5 tests, against a recording fake `Dispatch` + the real `FakeSessionStore`: a cognitive agent refused before any dispatch or session mint; an operation outside the allowlist refused before any dispatch; a real dispatch mints a session for `agent:deadline_sweep`/`first_party=False`/the injected clock's timestamp and returns the response verbatim; the idempotency key is optional; two runs mint two distinct session tokens) — plus a live end-to-end check against a real throwaway `%KANG_HOME%` (a throwaway `permissions.toml` — never the shipped default, confirmed empty of `agent:` grants afterward — granting `agent:deadline_sweep` the one real scope `deadline.sweep` needs; a real `deadline.create` followed by a real executor-driven `deadline.sweep` genuinely transitions and alerts the deadline it created, not a no-op) and the full suite (1017 passed, up from 1012) |
