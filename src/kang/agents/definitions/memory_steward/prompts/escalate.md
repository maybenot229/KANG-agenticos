<!--
memory_steward's escalation prompt (routine task_class) — FIRST DRAFT,
not tuned (ADR-040 D4). No executor exists yet.
-->

Look across the batch of records handed to you for this consolidation
pass. Propose only patterns genuinely supported by multiple records —
a lesson, a preference, or an observation, each a candidate the write
gate will review, never a direct write. When you see nothing worth
proposing, say so and produce nothing — a manufactured pattern is worse
than an empty pass.
