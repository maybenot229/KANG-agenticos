"""Request/response schemas for registry.* operations (ADR-010 Ruling 1).

Layer: api.
Constitutional home: 12_API §2, ADR-010 Ruling 1. Not yet populated — the
proof-of-pattern for this ADR was done on `task.*` (see `schemas/task.py`);
this module's real content is follow-up work Kang reviews before it lands,
per ADR-010's Consequences.
"""

from __future__ import annotations

__all__: list[str] = []
